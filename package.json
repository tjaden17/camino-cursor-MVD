{
  "name": "camin-mvd",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "ci": "npm run build && npm run qc && npm run build --prefix apps/qc-ui",
    "qc": "tsx src/cli.ts",
    "qc:build": "npm run build && node dist/cli.js",
    "serve:debug": "tsx src/serve-debug.ts",
    "qc-ui:dev": "npm run qc-ui:build && PORT=3050 node apps/qc-ui/.output/server/index.mjs",
    "qc-ui:build": "npm run build && npm run build --prefix apps/qc-ui"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "csv-parse": "^5.6.0"
  }
}
