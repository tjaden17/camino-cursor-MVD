# Zoho exports (v1.1)

Place **Zoho Desk** and **Zoho CRM** CSV or supported exports here. Filenames and column shapes may vary; pipeline adapters and normalisation are expected to evolve.

**Consumers:** KPI replay rules and QC (see `data/kpi-spec/kpi-spec-v1.json` and `npm run pipeline`).

**Do not** assume a single canonical filename yet — document any stable export you add in a short comment in your PR.
